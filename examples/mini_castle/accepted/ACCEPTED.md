# mini_castle — accepted iteration #3

- **verdict:** good
- **params digest:** `74078818`
- **generation:** `9df4c9c0` (hash of the acceptance criteria)
- **note:** The finished object against the revised criteria. Same geometry and digest as the printed mesh in accepted/. Printed, lit with the puck on its side, papered behind the face and the bat. The two open criteria were rewritten as the decisions they had become: the top turret is deliberately dark because the light is aimed at the face, and the opaque-base worry assumed an upright puck that the design no longer uses.

This directory is the deliverable, frozen. `model.py` here is the exact generator that produced this mesh — the one in the part directory has moved on. Do not edit anything in here; supersede it by accepting a later iteration.
