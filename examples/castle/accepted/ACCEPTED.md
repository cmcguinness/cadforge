# castle — accepted iteration #34

- **verdict:** printed
- **params digest:** `083466d5`
- **generation:** `942a7016` (hash of the acceptance criteria)
- **note:** Same parameters and digest as iteration #33 and as the mesh in accepted/. This entry exists only because reflowing spec.md re-wrapped the criteria and moved the generation hash — the exact failure the hash now normalises whitespace to prevent. Printed full size, lit, papered, past UAT.

This directory is the deliverable, frozen. `model.py` here is the exact generator that produced this mesh — the one in the part directory has moved on. Do not edit anything in here; supersede it by accepting a later iteration.
