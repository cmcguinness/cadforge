# pumpkin_lantern — accepted iteration #18

- **verdict:** good
- **params digest:** `a328657f`
- **generation:** `8be785f4` (hash of the acceptance criteria)
- **note:** printed and in the hand: reads as a jack-o'-lantern, face bright with the body glowing orange behind it, drops over the puck and stays centred, stem takes being picked up by. The interleaved 4-up/3-down grin reads as teeth — the diamond illusion from the aligned rows is gone.

This directory is the deliverable, frozen. `model.py` here is the exact generator that produced this mesh — the one in the part directory has moved on. Do not edit anything in here; supersede it by accepting a later iteration.
